package it.univaq.microsynth.Enum;


/**
 * Enum representing the different types of parameters that can be used in the system. This enum is used to define the type of a parameter, which can be a string, integer, float, boolean, object, array, or JSON. It helps in validating and processing parameters based on their type.
 */
public enum ParameterType {
    STRING,
    INTEGER,
    FLOAT,
    BOOLEAN,
    OBJECT,
    ARRAY,
    JSON
}
